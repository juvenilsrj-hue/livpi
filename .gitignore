# Sistema / temporários
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.~lock*

# Screenshots de verificação (gerados localmente, não publicar)
_shot_*.png
_h*.png
_v*.png
_final*.png
_preview_*.png

# RPI: downloads/cache pesados e binarios (nao versionar)
inpi-api/_amostra/
inpi-api/_cache/
inpi-api/cloudflared.exe
*.zip
*.xml
*.pdf

# Credenciais (nunca versionar)
.env
inpi-api/.env

# Backend Node (fora do site estatico; manter em repo proprio/privado)
inpi-api/

# Temporarios de render/preview
_*.png
_*.html
